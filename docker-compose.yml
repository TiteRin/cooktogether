services:
  cooktogether:
    build: .
    container_name: cooktogether
    ports:
      - "4321:4321"
    volumes:
      - ${RECIPES_PATH:-./recipes}:/app/recipes
    environment:
      - NODE_ENV=production
    restart: unless-stopped